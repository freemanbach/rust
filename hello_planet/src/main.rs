
#![allow(unused)]

// use rand::Rng;
use std::io;
use std::env;
use std::fs::File;
use std::path::Path;
use std::convert::TryInto;
use std::io::{Write, BufReader, BufRead, ErrorKind};
use std::cmp::Ordering;

fn main() {
    println!("Hello, Planet Earth!");
}
